---
- name: Create /plex-volumes directory
  file:
    path: /docker-volumes/plex
    state: directory
  become: True

- name: Create /plex-volumes/config directory
  file:
    path: /docker-volumes/plex/config
    state: directory
  become: True

- name: Create /plex-volumes/transcode directory
  file:
    path: /docker-volumes/plex/transcode
    state: directory
  become: True

- name: Create /plex-volumes/data directory
  file:
    path: /docker-volumes/plex/data
    state: directory
  become: True
